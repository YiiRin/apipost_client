import styled from 'styled-components'

export const Container = styled.div`
  margin-top: 1em;
  .proj-name {
    font-size: 14px;
    height: 40px;
    line-height: 40px;
    padding-left: 4px;
  }

  .locked {
    color: #ee6a5e;
  }
`
