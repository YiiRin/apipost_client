import React from 'react'
import { Container } from './index.style'

type Props = {
  /**
   * 确认框标题
   */
  title?: React.ReactNode
}

const Confirm: React.FC<Readonly<Props>> = (props) => {
  return <>index</>
}

export default Confirm
