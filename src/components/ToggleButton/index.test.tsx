import React from 'react'
import ToggleButton from '.'
type Props = {
}

const ToggleButtonTest: React.FC<Readonly<Props>> = (props) => {

    return (
        <ToggleButton>
        </ToggleButton>
    )
}

export default ToggleButtonTest